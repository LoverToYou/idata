# Phase 3: ETL 工作流编排 — 测试用例

## 前置条件

- 后端服务运行在 `http://localhost:8088`
- 存在至少一个 MySQL 数据源

---

## 1. 工作流 CRUD (后端 API)

| 编号 | 用例名 | 操作 | 预期结果 | 实际结果 | 状态 |
|------|--------|------|----------|----------|------|
| 3-01 | 创建工作流 | `POST /api/workflow/create {"name":"测试工作流","description":"描述"}` | 返回 200, status=DRAFT, dagJson="{}" | 正确返回 id=1, status=DRAFT, dagJson="{}" | ✅ |
| 3-02 | 创建工作流（空名称） | `POST /api/workflow/create {"name":""}` | 返回 400 校验错误 | - | 🔲 待测 |
| 3-03 | 列表查询 | `GET /api/workflow/list` | 返回工作流列表（不包含 dagJson） | 正确返回列表，dagJson=null | ✅ |
| 3-04 | 获取详情 | `GET /api/workflow/1` | 返回单条（包含 dagJson） | 正确返回包含 dagJson 字段 | ✅ |
| 3-05 | 更新工作流 | `PUT /api/workflow/update {"id":1,"name":"新名称","dagJson":"{...}"}` | 更新成功 | 正确返回更新后的 name 和 dagJson | ✅ |
| 3-06 | 更新不存在的 | `PUT /api/workflow/update {"id":999}` | 返回 400 错误 | 返回 "工作流不存在: 999" | ✅ |
| 3-07 | 发布工作流 | `POST /api/workflow/1/publish` | status 变为 PUBLISHED | 正确更新为 PUBLISHED | ✅ |
| 3-08 | 发布不存在的 | `POST /api/workflow/999/publish` | 返回 400 错误 | 返回 "工作流不存在: 999" | ✅ |
| 3-09 | 删除工作流 | `DELETE /api/workflow/1` | 删除成功 | 返回 200, data=null | ✅ |
| 3-10 | 删除不存在的 | `DELETE /api/workflow/999` | 返回 400 错误 | 返回 "工作流不存在: 999" | ✅ |
| 3-11 | 列表为空 | `GET /api/workflow/list` | 返回空数组 | - | 🔲 待测 |

**结论**: 工作流 CRUD API 全部正常工作。

---

## 2. DataX 配置生成 (后端)

| 编号 | 用例名 | 输入 | 预期结果 | 状态 |
|------|--------|------|----------|------|
| 3-12 | 生成 MySQL Reader 配置 | mysqlreader 配置 | 输出符合 DataX JSON 格式 | 🔲 单元测试 |
| 3-13 | 生成 Hive Writer 配置 | hivewriter 配置 | 输出符合 DataX JSON 格式 | 🔲 单元测试 |
| 3-14 | 生成完整 Job 配置 | source + sink 节点 | 输出完整的 job 配置 | 🔲 单元测试 |

---

## 3. 前端页面渲染 (手动测试)

| 编号 | 用例名 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|------|--------|----------|----------|----------|------|
| 3-15 | 工作流列表页 | 访问 `/workflow` | 显示表格 + 新建按钮 + 搜索框 | 表格列完整，空状态显示"暂无工作流" | ✅ |
| 3-16 | 新建工作流页面 | 点击"新建工作流" | 跳转到编辑器页面 | URL: `/workflow/create`，三个面板正常渲染 | ✅ |
| 3-17 | 节点模板面板 | 观察左侧面板 | 显示 4 种节点模板 | MySQL Reader/Hive Reader/MySQL Writer/Hive Writer 均显示 | ✅ |
| 3-18 | Vue Flow 画布 | 观察中间区域 | 画布渲染 + 缩放控件 | 画布正常显示，带 zoom in/out/fit 按钮 | ✅ |
| 3-19 | 属性面板 | 观察右侧面板 | 未选择节点时显示空状态 | 显示"选择节点以配置属性"空状态 | ✅ |
| 3-20 | 顶部工具栏 | 观察顶部栏 | 返回按钮 + 名称输入 + 保存/运行按钮 | 所有按钮正常显示 | ✅ |
| 3-21 | 拖拽节点到画布 | 拖拽 MySQL Reader 到画布 | 节点出现在画布上 | - | 🔲 待测 (需要 Playwright drag) |
| 3-22 | 连接两个节点 | 从 Source 拖到 Sink | 生成连线 | - | 🔲 待测 |
| 3-23 | 点击节点 | 点击画布上的节点 | 属性面板显示配置表单 | - | 🔲 待测 |
| 3-24 | 属性编辑 | 在属性面板编辑配置 | 节点配置更新 | - | 🔲 待测 |
| 3-25 | 删除节点 | 点击"删除节点"按钮 | 节点从画布移除 | - | 🔲 待测 |
| 3-26 | 保存工作流 | 填写名称后点击保存 | 调用 API 保存，跳转到编辑 URL | - | 🔲 待测 |
| 3-27 | 编辑已有工作流 | 点击编辑已有工作流 | 加载 DAG 数据到画布 | - | 🔲 待测 |

---

## 4. 已知限制

| 编号 | 问题 | 说明 | 建议 |
|------|------|------|------|
| B-06 | DataX 功能需要本地安装 DataX | DataXRunner 依赖 `/opt/datax` 路径 | 需部署 DataX 后验证 |
| B-07 | DataX 并发执行控制 | 无限制，同时执行多个可能资源竞争 | 后续可加入执行队列 |
| B-08 | 列映射使用文本输入 | 当前用 textarea 简单处理 | 后续可改为可视化字段映射 |

## 测试环境信息

- 后端版本: Spring Boot 3.2.5 + JDK 25
- 前端版本: Vue 3 + Vite 5
- 数据库: MySQL 8.0
- 测试日期: 2026-04-30
- 测试工具: curl / 浏览器手动测试
